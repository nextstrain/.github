name: CI

on:
  - push
  - pull_request
  - workflow_dispatch

jobs:
  test-pathogen-repo-ci-augur:
    uses: ./.github/workflows/pathogen-repo-ci.yaml
    with:
      repo: nextstrain/zika
      augur-version: master
      runtimes: |
        - conda
    secrets: inherit
