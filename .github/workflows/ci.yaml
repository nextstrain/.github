name: CI

on:
  push:
  pull_request:

jobs:
  test-pathogen-repo-ci-augur:
    uses: ./.github/workflows/pathogen-repo-ci.yaml
    with:
      repo: nextstrain/ncov
      augur-version: remove-overeager-validation
      build-args: all_regions -j 2 --profile nextstrain_profiles/nextstrain-ci
      runtimes: |
        - conda
    secrets: inherit
